actions:
  # This will be rendered with contexts
  - action: file.copy
    from: some-file
    to: some-path
    template: true

  # This will not be rendered with contexts
  - action: file.copy
    from: some-file
    to: some-path
